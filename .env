# ============================
# MoonDust Stack - .env File
# ============================

# --- Port Configurations ---
NGINX_PORT=80               # NGINX external
KEYCLOAK_PORT=8081          # Keycloak external (internal remains 8080)
POSTGRES_PORT=5432          # Only expose externally if needed

# --- PostgreSQL Config ---
POSTGRES_DB=moondust
POSTGRES_USER=moondust
POSTGRES_PASSWORD=moondust  # ⚠️ Use secrets for production

# --- PostgreSQL Schema Names (Optional) ---
POSTGRES_SCHEMA_SYS=md_sys
POSTGRES_SCHEMA_APP=md_app
POSTGRES_SCHEMA_KEYCLOAK=keycloak

# --- Keycloak Bootstrap Admin ---
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=admin  # ⚠️ Change in non-dev environments

# --- Keycloak URL (for apps; not evaluated here) ---
# NOTE: this won't expand ${KEYCLOAK_PORT} in Docker Compose
# Use it in app runtime config instead
# Example use: Spring Boot, Angular config, etc.
# KEYCLOAK_BASE_URL=http://localhost:8081/keycloak
